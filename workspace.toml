[workspace]
# This file configures the workspace-wide tools and settings for the Ragas monorepo
# All code quality tools (ruff, pyright) inherit from these centralized settings
# Individual projects only override when absolutely necessary for package-specific needs

[tool.ruff]
select = ["E", "F", "I"]
ignore = ["E501"]  # Line length handled by formatter
line-length = 88
target-version = "py39"
exclude = ["*.ipynb"]  # Exclude Jupyter notebooks from linting

[tool.ruff.lint.isort]
# Import sorting configuration for the entire monorepo
known-first-party = ["ragas"]
force-single-line = false
combine-as-imports = true

[tool.ruff.format]
line-length = 88
target-version = "py39"
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
docstring-code-format = false
preview = false

[tool.pyright]
include = ["ragas/src/ragas"]
excludeTypeshedPaths = ["@types/*"]
pythonVersion = "3.9"
pythonPlatform = "All"
typeCheckingMode = "basic"
reportMissingImports = false
reportOptionalMemberAccess = "warning"
reportOptionalSubscript = "warning"
reportGeneralTypeIssues = "warning"
reportReturnType = "warning"

[tool.pytest.ini_options]
addopts = "-n 0"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["ragas/tests"]